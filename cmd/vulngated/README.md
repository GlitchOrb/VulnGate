# vulngated (Planned)

This directory is reserved for the future VulnGate server mode.

The MVP ships a CLI-first single-binary workflow (`cmd/vulngate`).
Server capabilities (multi-tenant graph queries, GUAC ingestion, org policy APIs)
will be introduced incrementally without breaking CLI compatibility.
